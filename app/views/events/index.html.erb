<h1>Events</h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <%= link_to event.title, event %> <small>Time: </small>
      <%= event.date%>
    </li>
  <% end %>
</ul>

<%= link_to "New event", new_event_path %>